import { StringPipPipe } from './string-pip.pipe';

describe('StringPipPipe', () => {
  it('create an instance', () => {
    const pipe = new StringPipPipe();
    expect(pipe).toBeTruthy();
  });
});
